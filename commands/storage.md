---
description: View or change Mirror storage mode
argument-hint: "[local_chroma|mem0_cloud]"
allowed-tools: Bash
---

View or change Mirror storage:

```bash
python3 -m uv run --project "${CLAUDE_PLUGIN_ROOT}" mirror storage $ARGUMENTS
```
